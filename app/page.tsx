import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div className="flex gap-4">
      <Button className="text-md font-semibold bg-gray-800">Test</Button>
    </div>
  );
};

export default page;
